#!/usr/bin/expect -f
set timeout 30
set host "158.220.127.50"
set port "2222"
set user "root"
set password "m0uvCboqJ50ro6hNQ5Cc"

spawn scp -P $port -o StrictHostKeyChecking=no setup_admin_screensaver.sh $user@$host:/root/setup_admin_screensaver.sh
expect {
    "password:" {
        send "$password\r"
        exp_continue
    }
    eof
}

spawn ssh -p $port -o StrictHostKeyChecking=no $user@$host "bash /root/setup_admin_screensaver.sh"
expect {
    "password:" {
        send "$password\r"
        exp_continue
    }
    eof
}
