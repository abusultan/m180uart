#!/usr/bin/expect -f
set timeout 30
set host "158.220.127.50"
set port "2222"
set user "root"
set password "m0uvCboqJ50ro6hNQ5Cc"

# First, scp the file
spawn scp -P $port -o StrictHostKeyChecking=no deploy_screensaver.sh $user@$host:/root/deploy_screensaver.sh
expect {
    "password:" {
        send "$password\r"
        exp_continue
    }
    eof
}

# Then ssh in and execute
spawn ssh -p $port -o StrictHostKeyChecking=no $user@$host "bash /root/deploy_screensaver.sh"
expect {
    "password:" {
        send "$password\r"
        exp_continue
    }
    eof
}
